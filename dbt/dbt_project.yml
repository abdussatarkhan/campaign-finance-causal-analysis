name: "campaign_finance_models"
version: "1.0.0"
config-version: 2

profile: "campaign_finance"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  campaign_finance_models:
    staging:
      +materialized: view
    marts:
      +materialized: table
